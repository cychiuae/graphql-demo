type User {
  id: ID!
  name: String!
}

type Message {
  id: ID!
  createdAt: Int!
  content: String!
  sender: User!
}

type Conversation {
  id: ID!
  topic: String!
  messages: [Message!]!
}

type Query {
  user(id: ID!): User
}
